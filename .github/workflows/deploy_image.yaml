name: Docker Build and Push

on:
  push:
    branches:
      - main  # Altere para a branch principal do seu repositório

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Login to Docker Hub
      run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login --username "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

    - name: Build and push Docker image
      run: |
        docker build -t felipe070700/whatsapp-manager .
        docker push felipe070700/whatsapp-manager

    - name: Send POST request
      run: |
        curl -X POST -d '{"key":"value"}' -H "Content-Type: application/json" http://5.161.71.166:9000/api/stacks/webhooks/a2c64dae-1894-4377-acc8-ed91d8cfdc70
